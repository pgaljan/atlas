import React from "react";
import Layout from "../../../components/layout";

const Home = ({ onSubmit }) => {
  return (
    <>
      <Layout onSubmit={onSubmit} />
    </>
  );
};

export default Home;
